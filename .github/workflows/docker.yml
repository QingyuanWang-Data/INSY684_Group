name: Docker Build

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build API image
        run: docker build -t insy684-homecredit-service:ci .
